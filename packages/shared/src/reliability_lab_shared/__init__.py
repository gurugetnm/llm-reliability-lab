"""Shared types and utilities used across `packages/*` and `apps/api`.

Empty for now — nothing has needed to be shared yet. Add to this package
only when a type or helper is genuinely duplicated across two or more
packages; don't pre-populate it speculatively.
"""

__version__ = "0.1.0"
