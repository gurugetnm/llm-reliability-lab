# reliability-lab-shared

Shared types and utilities used across `packages/*` and `apps/api`. Kept
empty until something is genuinely duplicated across two or more
packages — this is not a dumping ground for speculative abstractions.
