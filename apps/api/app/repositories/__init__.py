"""Thin data-access functions between services and the database.

Kept as plain functions (not classes) to match the rest of the app's
style (see `app/services/project_service.py`) — a repository here is
"the queries a service needs," not a framework.
"""
